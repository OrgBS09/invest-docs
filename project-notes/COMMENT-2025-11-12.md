# COMMENT — 2025-11-12

## Что настроили
- Git ветки: `lab` (default) → `dev` → `prod`; старые ветки очищены.
- Публичная публикация доков: `invest-services` →(doc/**)→ `invest-docs` (main) через GitHub Actions.
- Секрет `DOCS_PUSH_TOKEN` (PAT) выдан только на `invest-docs`, права Contents: R/W.
- На HUB установлен каркас Diátaxis и ADR, принята схема переноса контекста в `/doc`.
- Для personal-среды поднят systemd-таймер автокоммита/пуша (HTTPS+PAT).

## Как пользоваться (коротко)
- Кладём материалы проекта в `/doc/**` и делаем `git commit && git push` в ветку `lab`.
- Action автоматически синканёт содержимое в `OrgBS09/invest-docs` (ветка `main`).

## Контуры
- Проектовые публичные материалы → invest-docs.
- Личные приватные знания/скрипты → personal-services.
- Данные для Vector Store → /home/ubuntu/lab/invest/knowledge/** (обслуживается твоими скриптами).
