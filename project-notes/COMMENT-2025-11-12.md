# COMMENT — 2025-11-12

## Что настроили
- Ветки: `lab` (default) → `dev` → `prod`; старые ветки очищены.
- Публикация доков: `invest-services/doc/**` → `invest-docs` (ветка `main`) через GitHub Actions.
- Секрет `DOCS_PUSH_TOKEN` (Fine-grained PAT) выдан на `invest-docs` с правами Contents: Read/Write.
- На HUB принят каркас Diátaxis и ADR. Контекст переносим в `/doc`.
- В personal-среде работает systemd-таймер автокоммитов/пушей (HTTPS+PAT/.netrc).

## Как пользоваться (коротко)
- Материалы проекта кладём в `/doc/**` → `git add/commit/push` в ветку `lab`.
- Action автоматически синхронизирует содержимое в `OrgBS09/invest-docs` (ветка `main`).

## Контуры
- Публичные материалы проекта → invest-docs.
- Личные/приватные знания и скрипты → personal-services.
- Данные для Vector Store → `/home/ubuntu/lab/invest/knowledge/**` (индексируют ваши скрипты).
