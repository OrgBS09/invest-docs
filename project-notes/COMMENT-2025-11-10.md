# COMMENT — итог на сегодня

## Что сделано
- Репозиторий: OrgBS09/invest-services.
- Ветки: создано `lab` (default), `dev`, `prod`; пуш с HUB настроен по SSH.
- Каркас: добавлены doc/ (Diátaxis), ADR-0001, env.example, pyproject/ruff/black/pytest.
- Доступы: аккаунт *BService-invest* — админ в OrgBS09; SSH-ключ зарегистрирован.
- Следующий блок: защита `dev`/`prod`, шаблоны PR/Issue, CODEOWNERS, публикация Pages из `prod`.

## Решения (кратко)
- Модель ветвления: lab → dev → prod (через PR).
- Документация: Diátaxis, ADR формата Nygard.
- Секреты: только окружение/Environments, не храним в git.

## NEXT (локально)
- Включить Protected Branches для `dev`/`prod`.
- Добавить PR/Issue шаблоны и CODEOWNERS.
- Подключить GitHub Pages (Source: GitHub Actions) для папки `doc/` из ветки `prod`.
